---
# Dynamic fields whose values vary between runs; excluded from comparison.
dynamic_fields:
  "@timestamp": ".*"
  event.ingested: ".*"
  event.created: ".*"
  agent.ephemeral_id: ".*"
  agent.id: ".*"
  agent.name: ".*"
  agent.version: ".*"
  elastic_agent.id: ".*"
  elastic_agent.version: ".*"
