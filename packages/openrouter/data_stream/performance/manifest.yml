title: "performance"
type: metrics
streams:
  - input: cel
    title: OpenRouter API performance metrics
    description: >-
      Collect hourly non-additive performance metrics (latency percentiles, throughput,
      cache hit rate, guardrail rate) from the OpenRouter Analytics API, grouped by
      model and provider.
    template_path: cel.yml.hbs
    vars:
      - name: interval
        type: text
        title: Collection interval
        description: How often the Analytics API is polled for new hourly data.
        required: true
        show_user: true
        default: 6h
      - name: initial_interval
        type: text
        title: Initial lookback interval
        description: >-
          How far back to collect data on the first run (e.g. 168h = 7 days).
          Must be a valid Go duration string.
        required: true
        show_user: true
        default: 168h
      - name: finalization_delay
        type: text
        title: Finalization delay
        description: >-
          How far behind "now" the collection window end is kept, to allow
          partially-finalized hour buckets to settle before being ingested.
        required: true
        show_user: false
        default: 2h
      - name: dimensions
        type: text
        title: Dimensions
        description: >-
          Dimensions to group performance data by. Maximum 2 dimensions.
          Supported: model, variant, api_key_id, workspace, app, user, provider,
          origin, country, data_region, streamed, finish_reason.
        multi: true
        required: true
        show_user: true
        default:
          - model
          - provider
      - name: http_client_timeout
        type: text
        title: HTTP client timeout
        description: Duration to wait before declaring a request timed out (e.g. 60s).
        required: false
        show_user: false
        default: 60s
      - name: proxy_url
        type: text
        title: Proxy URL
        required: false
        show_user: false
      - name: ssl
        type: yaml
        title: SSL configuration
        required: false
        show_user: false
      - name: tags
        type: text
        title: Tags
        multi: true
        required: true
        show_user: false
        default:
          - forwarded
      - name: preserve_original_event
        type: bool
        title: Preserve original event
        required: true
        show_user: true
        default: false
      - name: processors
        type: yaml
        title: Processors
        required: false
        show_user: false
        description: Processors applied to events before ingest pipeline parsing.
elasticsearch:
  index_template:
    mappings:
      subobjects: false
